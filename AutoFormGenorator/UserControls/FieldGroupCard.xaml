<UserControl x:Class="AutoFormGenorator.UserControls.FieldGroupCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AutoFormGenorator.UserControls"
             mc:Ignorable="d" 
        d:DesignHeight="300" HorizontalAlignment="Stretch">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="DisplayNameTextBlock" Foreground="#757575" TextAlignment="Left" FontSize="20" FontWeight="Bold" HorizontalAlignment="Stretch" TextWrapping="Wrap" Text="" VerticalAlignment="Top" Grid.Row="0"  Height="auto" Width="Auto"/>

        <Separator Grid.Row="1" Grid.ColumnSpan="3" VerticalAlignment="Top"/>

        <WrapPanel x:Name="ControlsWrapPanel" Grid.Row="2" Margin="10,10,10,0" >

        </WrapPanel>
    </Grid>
</UserControl>
